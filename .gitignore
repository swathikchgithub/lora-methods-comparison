# Python
__pycache__/
*.pyc
.pytest_cache/
venv/
.venv/

# Model weights & checkpoints (large binaries — never commit; published to HF Hub instead)
checkpoints/
adapters/
*.safetensors
*.bin
*.pt
*.gguf

# Results that contain raw model outputs (kept out of git; summarized in README instead)
results/*/raw_outputs/

# Secrets
.env
*.key

# OS
.DS_Store
