# Core fine-tuning stack (run on a CUDA GPU box, e.g. RunPod)
# torch/bitsandbytes pinned to versions with Blackwell (sm_120) kernel
# support - 0.43.3/2.4.1 failed with "no kernel image is available for
# execution on the device" on an RTX PRO 4500 Blackwell; verified working
# combination below. If you're on an older-architecture GPU (Ampere/Ada),
# these versions still work fine - no need to downgrade.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0
transformers==4.44.2
peft==0.12.0
bitsandbytes==0.50.2
accelerate==0.34.2
datasets==2.21.0
trl==0.9.6
rich==13.8.1        # transitive dep of trl's SFTTrainer utils, not auto-installed
tensorboard==2.17.1 # SFTConfig(report_to=["tensorboard"]) requires this explicitly

# Publishing trained adapters to Hugging Face Hub
huggingface_hub==0.36.2

# Eval / comparison harness (runs anywhere, including a laptop, once results are pulled down)
scikit-learn==1.5.1
pandas==2.2.2
numpy==1.26.4
matplotlib==3.9.2
tqdm==4.66.5

# Dev / test
pytest==8.3.2
